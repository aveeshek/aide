# Stale knowledge report

No stale canonical pages are recorded in the starter data.

Production CI should flag pages whose `last_verified_at`, source commit, contract pointer, or code symbol no longer resolves.
